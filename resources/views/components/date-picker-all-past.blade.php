<input type="text" name="{{ $name }}" id="{{ $id }}" value="{{ $value }}"
    class="flatpickr-date-only block w-full py-2 sm:py-3 px-3 sm:px-4 border border-gray-300 rounded-lg text-sm focus:ring-2 focus:ring-yellow-500 focus:border-yellow-500 bg-white"
    placeholder="{{ $placeholder }}">
